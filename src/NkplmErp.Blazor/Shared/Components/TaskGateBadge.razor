@* Header entry point back into the gate. Only appears when the user has tasks
   they have not started, and only while the gate itself is closed. *@

@if (State.PendingCount > 0 && !State.IsOpen)
{
    @* <button type="button" class="tgb-badge" @onclick="ReplayAsync"
            title="Review your pending tasks">
        <i class="fa-solid fa-list-check" aria-hidden="true"></i>
        <span>@State.PendingCount</span>
    </button> *@
}
